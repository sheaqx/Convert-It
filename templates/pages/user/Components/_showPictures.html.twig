<div class="user-pictures my-4">
	<ul class="list-unstyled row row-cols-2 row-cols-lg-4 row-cols-md-2 g-2 g-lg-3">
		{% for picture in app.user.pictures %}
			<li class="col">
				<form class="card d-flex-column h-100" method="POST">
					<div class="m-2">
						<h6 class="card-title">{{picture.tag}}</h6>
						<div class="img-container">
							<img class="img-fluid" src="{{asset('files/pictures/')}}{{ picture.name }}" alt="{{picture.tag}}"/>
						</div>
						<span>{{picture.description}}</span>
					</div>
					<input type="hidden" value="{{picture.id}}" name="pictureID">
					<button type="submit" class="btn btn-secondary w-50 align-self-center m-2">Supprimer</button>
				</form>
			</li>
		{% endfor %}
	</ul>
</div>
